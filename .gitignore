# dependencies
node_modules/

# build output
dist/
coverage/
.vite/

# logs
*.log
npm-debug.log*

# env / secrets (AdMob prod IDs, signing keys live here — never commit)
.env
.env.*
!.env.example

# os / editor
.DS_Store
Thumbs.db
*.local
.idea/
.vscode/*
!.vscode/extensions.json

# machine-local Claude Code settings (permissions etc.)
.claude/settings.local.json

# capacitor native build artifacts (added Phase 16)
android/app/build/
android/build/
android/.gradle/
android/local.properties
android/keystore.properties
ios/App/build/
*.keystore
*.jks
