# Normalize line endings: store LF in the repo, check out native on each OS.
* text=auto eol=lf

# Binary assets (added in later phases) must not be normalized.
*.png binary
*.jpg binary
*.glb binary
*.gltf binary
*.ttf binary
*.keystore binary
